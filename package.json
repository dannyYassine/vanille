{
  "name": "vanille",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.9",
    "vitest": "^0.34.3"
  }
}
